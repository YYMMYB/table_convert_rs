using System.IO;

namespace {{common_namespace}};

public interface IDataAccess {
    IDataPath RootPath();
    IDataPath JoinPath(IDataPath path, string item);
    string GetData(IDataPath path);
}

public interface IDataPath {

}